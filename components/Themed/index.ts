import {
  useThemeColor,
  Text as ThemedText,
  View as ThemedView,
  type TextProps as ThemedTextProps,
  type ViewProps as ThemedViewProps,
} from "./Themed";

export { useThemeColor, ThemedText, ThemedView, ThemedTextProps, ThemedViewProps };
